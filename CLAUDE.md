# Claude Code Entry Point

This repository is the Sheikh Ammar bin Humaid Al Nuaimi private royal horology museum. Read `docs/CLAUDE_CODE_HANDOFF.md` before changing code. It contains the repository map, exact commands, bilingual layout rules, project-only media policy, migration safety rules, GitHub workflow, validation sequence, and rollback procedure.

Use the managed project source as the application truth. Preserve the historic dark-luxury composition, Arabic RTL / English LTR image placement, source-bounded editorial wording, hidden administrator boundary, and non-commercial museum presentation.

Never commit `.env` files, API keys, database credentials, JWT secrets, OAuth secrets, storage credentials, GitHub tokens, administrator passwords, prices, market values, fabricated testimonials, or unsupported ownership claims. Run `node scripts/verify-handoff.mjs`, `pnpm check`, `pnpm test`, and `pnpm build` before proposing a merge. For a new feature, add a checked evidence item to `todo.md` only after the change and tests are complete.
