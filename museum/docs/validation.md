# Validation — 9 September 2026

14 Node tests passed on current edition: six route/language startup cases, all 42 modal names and image paths in both languages, filters/search/empty/reset/zoom/RTL arrows/Escape, featured controls/focus, reduced-motion defaults, lazy YouTube creation, load-failure retry, HTTP routes/health, video byte ranges/HEAD/ETag and private-path/write rejection. npm run build passed as a Vite diagnostic; production serves authored dist directly. This does not claim the managed React app’s tests passed.

All 42 records have assigned local media and bilingual editorial fields. Assignment is not a new independent verification of every photograph’s identification. Previous internal media notes retain context for unresolved reference questions.

Fixed a startup null-element binding, restored actual Top 3 autoplay, added YouTube Air31Kly7Ys from the managed collection, retained the local MP4, and separated the mobile opening photo from text. Removed unused unrelated Library photos and screenshots from the public folder. User originals remain untouched.

Live browser: restored six home cards and three featured items; 360px iframe content measured 345px with equal scroll width. Resize-matrix evaluation intermittently timed out. Full browser matrix, YouTube playback, and Lighthouse remain outstanding; no 10/10 claim.

Source audit: main 55cd003; PR22 draft, PR23 open, PR24 draft, PR25 open non-draft, all unmerged. This branch only adds museum/, a museum CI workflow and Railway configuration; managed application and existing docs website preserved. A main merge requires the managed validation gate separately.
